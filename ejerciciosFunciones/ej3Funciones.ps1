<# Ejercicio 3
Implementa un programa en PowerShell que funcione como una calculadora básica. El
programa debe incluir funciones para realizar operaciones de suma, resta, multiplicación y
división. Además, deberá mostrar un menú al usuario con las opciones disponibles y
permitirle realizar operaciones hasta que elija salir. Asegúrate de manejar correctamente la
división por cero y mostrar un mensaje de error en ese caso.#>[String]$Funcion = Read-Host -Prompt "Elige cual de las siguientes funciones de la calculador vas a usar: | Sumar | Restar | Multiplicar | Dividir |"[String]$Eleccion = "Continuar"while($Eleccion -eq "Continuar"){    if($Funcion -eq "Sumar"){        [Int]$NumX = Read-Host -Prompt "Elige el primer numero"        [Int]$NumY = Read-Host -Prompt "Elige el segundo numero"        Sumar $NumX $NumY        [String]$Eleccion = Read-Host -Prompt "Quieres Continuar o quieres Salir?"        if($Eleccion -eq "Continuar"){$Funcion = Read-Host -Prompt "Que operacion quieres hacer ahora?"}else{Write-Host "Adios..."}    }elseif ($Funcion -eq "Restar"){        [Int]$NumX = Read-Host -Prompt "Elige el primer numero"        [Int]$NumY = Read-Host -Prompt "Elige el segundo numero"        Restar $NumX $NumY        [String]$Eleccion = Read-Host -Prompt "Quieres Continuar o quieres Salir?"        if($Eleccion -eq "Continuar"){$Funcion = Read-Host -Prompt "Que operacion quieres hacer ahora?"}else{Write-Host "Adios..."}    }elseif($Funcion -eq "Multiplicar"){        [Int]$NumX = Read-Host -Prompt "Elige el primer numero"        [Int]$NumY = Read-Host -Prompt "Elige el segundo numero"        Multiplicar $NumX $NumY        [String]$Eleccion = Read-Host -Prompt "Quieres Continuar o quieres Salir?"        if($Eleccion -eq "Continuar"){$Funcion = Read-Host -Prompt "Que operacion quieres hacer ahora?"}else{Write-Host "Adios..."}    }elseif($Funcion -eq "Dividir"){        [Int]$NumX = Read-Host -Prompt "Elige el primer numero"        [Int]$NumY = Read-Host -Prompt "Elige el segundo numero"        if($NumY -eq 0){            Write-Host "Error no se puede dividir por 0"        }        else{            Dividir $NumX $NumY        }        [String]$Eleccion = Read-Host -Prompt "Quieres Continuar o quieres Salir?"        if($Eleccion -eq "Continuar"){$Funcion = Read-Host -Prompt "Que operacion quieres hacer ahora?"}else{Write-Host "Adios..."}    }}Function Sumar($x,$y){    Write-Host ($x+$y)}Function Restar($x,$y){    Write-Host ($x-$y)}Function Multiplicar($x,$y){    Write-Host ($x*$y)}Function Dividir($x,$y){    Write-Host ($x/$y)}